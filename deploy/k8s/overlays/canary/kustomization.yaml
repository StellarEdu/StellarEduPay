apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

bases:
  - ../../

replicas:
  - name: backend
    count: 1

patchesStrategicMerge:
  - backend-patch.yaml

commonLabels:
  environment: canary
